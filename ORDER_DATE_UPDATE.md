# 订单日期范围更新

## 📅 更新说明

根据用户需求，订单数据爬取的日期范围已从**过去7天**更改为**未来7天**。

---

## 🔄 变更详情

### 之前（旧版本）
- **日期范围**：过去7天（从今天往前推7天）
- **示例**：今天是 2025-11-15，爬取 2025-11-08 到 2025-11-15
- **文件名**：`orders-20251115.csv`

### 现在（新版本）✅
- **日期范围**：未来7天（从今天开始往后7天）
- **示例**：今天是 2025-11-15，爬取 2025-11-15 到 2025-11-21
- **文件名**：`orders-future-20251115-to-20251121.csv`

---

## 📝 代码变更

### 1. `auto_crawl.py`

#### 变更前：
```python
# 计算日期范围
end_date = date.today()
start_date = end_date - timedelta(days=days-1)
```

#### 变更后：
```python
# 计算日期范围：从今天开始的未来7天
start_date = date.today()
end_date = start_date + timedelta(days=days-1)
```

### 2. `crawl_orders.py`

同样的变更应用到独立的订单爬取脚本。

### 3. 文件名变更

#### 变更前：
```python
csv_path = export_dir / f"orders-{end_date.strftime('%Y%m%d')}.csv"
```

#### 变更后：
```python
csv_path = export_dir / f"orders-future-{start_date.strftime('%Y%m%d')}-to-{end_date.strftime('%Y%m%d')}.csv"
```

---

## 🎯 使用场景

### 未来7天预订（新版本）✅
**适用于**：
- 📊 查看即将到来的预订情况
- 📈 预测未来一周的场地使用率
- 💰 估算未来一周的收入
- 📅 提前规划场地安排
- 👥 了解未来客户预订情况

**示例数据**：
```csv
order_id,order_no,date,time,customer_name,price,status
1662391,T2511142303516329850114,20251116,08:00-09:00,张三,86.4,已支付
1655476,T2511111859193670110846,20251117,19:00-20:00,李四,102.4,已支付
```

### 过去7天订单（旧版本）
**适用于**：
- 📊 查看历史订单记录
- 💰 统计过去一周的收入
- 📈 分析历史数据趋势

---

## 📦 文件输出示例

### 新版本输出文件名格式

```
data/exports/orders-future-20251115-to-20251121.csv
```

**文件名解读**：
- `orders-future-` - 表示未来订单
- `20251115` - 起始日期（今天）
- `to` - 连接符
- `20251121` - 结束日期（7天后）
- `.csv` - CSV 格式

### 旧版本输出文件名格式（已废弃）

```
data/exports/orders-20251115.csv
```

---

## 🔧 如何使用

### 方法 1：一键运行（推荐）

```bash
python3 auto_crawl.py
```

自动完成：
1. ✅ 登录
2. ✅ 提取 tokens
3. ✅ 爬取场地数据
4. ✅ 爬取未来7天订单数据 ← **已更新**
5. ✅ 保存 CSV

### 方法 2：单独爬取订单

```bash
python3 crawl_orders.py
```

需要先有 `login_tokens.json` 文件。

---

## 📊 数据示例

### 未来7天预订数据

```csv
order_id,order_no,normal_no,date,time,field_name,field_timebucket,customer_name,customer_nickname,customer_phone,price,status,order_type,confirmation_code,is_member
1662391,T2511142303516329850114,T2511142303515103182044-YA48,20251116,08:00-09:00,预订[Hehaa嘿哈网球学练馆]1片场地[学练馆][01]08:00-09:00,[学练馆][01]8:00-9:00,张三,小张,13800138000,86.4,已支付,普通订单,937968,是
1655476,T2511111859193670110846,T2511111859195956312940-YA88,20251117,19:00-20:00,预订[Hehaa嘿哈网球学练馆]1片场地[学练馆][01]19:00-20:00,[学练馆][01]19:00-20:00,李四,小李,13900139000,102.4,已支付,普通订单,153007,是
```

### 数据字段说明

| 字段 | 说明 | 示例 |
|------|------|------|
| `order_id` | 订单 ID | 1662391 |
| `order_no` | 订单编号 | T2511142303516329850114 |
| `date` | 预订日期 | 20251116 |
| `time` | 预订时间 | 08:00-09:00 |
| `customer_name` | 客户姓名 | 张三 |
| `customer_phone` | 客户电话 | 13800138000 |
| `price` | 价格 | 86.4 |
| `status` | 状态 | 已支付/待支付/已完成/已取消 |
| `order_type` | 订单类型 | 普通订单/会员订单 |
| `is_member` | 是否会员 | 是/否 |

---

## 💡 常见问题

### Q1: 为什么要改成未来7天？
**A**: 根据用户需求，需要查看**即将到来的预订情况**，而不是历史订单。这样可以：
- 提前了解场地使用情况
- 预测未来收入
- 提前做好场地安排

### Q2: 如果我想要过去7天的数据怎么办？
**A**: 可以修改代码中的日期计算逻辑：

```python
# 改回过去7天
end_date = date.today()
start_date = end_date - timedelta(days=days-1)
```

### Q3: 可以自定义天数吗？
**A**: 可以！修改 `crawl_orders` 函数的 `days` 参数：

```python
# 爬取未来14天
crawl_orders(tokens, days=14)
```

或者在调用时指定：

```python
# 在 auto_crawl.py 的 main 函数中
if crawl_orders(tokens, days=14):  # 改为14天
    print("✓ 订单数据爬取成功")
```

### Q4: 日期范围包含今天吗？
**A**: 是的！日期范围是**包含今天**的：
- 起始日期：今天（2025-11-15）
- 结束日期：今天 + 6 天（2025-11-21）
- 总共：7天

---

## 📚 相关文档

- [订单爬取指南](ORDER_CRAWL_GUIDE.md) - 详细使用说明
- [一键运行指南](ONE_CLICK_GUIDE.md) - 快速开始
- [README](readme.md) - 项目总览

---

## 🔄 更新日志

- **2025-11-15**: 将订单日期范围从"过去7天"改为"未来7天"
- **2025-11-15**: 更新文件名格式为 `orders-future-YYYYMMDD-to-YYYYMMDD.csv`
- **2025-11-15**: 更新所有相关文档和注释


