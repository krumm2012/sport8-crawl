# 双向同步实施路线图

## 架构概览

```
                    ┌─────────────────────────────────────┐
                    │         用户/管理员操作               │
                    │    (bakewell.cloud 管理后台)          │
                    └─────────────┬───────────────────────┘
                                  │
                    ┌─────────────▼───────────────────────┐
                    │      基准数据源 (Master)              │
                    │  ┌─────────────────────────────┐    │
                    │  │  bakewell.cloud             │    │
                    │  │  124.223.13.170             │    │
                    │  └─────────────────────────────┘    │
                    └─────────────┬───────────────────────┘
                                  │ 变更检测
                    ┌─────────────▼───────────────────────┐
                    │         同步协调中心                   │
                    │  ┌─────────┐ ┌─────────┐ ┌────────┐ │
                    │  │状态管理 │ │冲突解决 │ │操作队列 │ │
                    │  └─────────┘ └─────────┘ └────────┘ │
                    └─────────────┬───────────────────────┘
                                  │ 操作指令
        ┌─────────────────────────┼─────────────────────────┐
        │                         │                         │
        ▼                         ▼                         ▼
┌───────────────┐       ┌───────────────┐       ┌───────────────┐
│   Sport8      │       │    Meituan    │       │    审计日志    │
│   操作器       │       │    操作器      │       │               │
│  (Selenium)   │       │  (Selenium)   │       │               │
└───────────────┘       └───────────────┘       └───────────────┘
```

## 核心组件

### 1. StateManager (状态管理器)
```python
# 维护单一事实来源
- SQLite 数据库存储
- 版本控制 (乐观锁)
- 状态历史记录
```

### 2. ConflictResolver (冲突解决器)
```python
# 解决策略
- 以 bakewell 为准
- 时间优先
- 手动干预
```

### 3. OperationQueue (操作队列)
```python
# 可靠执行
- 优先级队列
- 指数退避重试
- 死信队列
```

### 4. Operator (操作执行器)
```python
# 平台适配
- Sport8Operator (页面操作)
- MeituanOperator (页面操作)
```

## 操作映射表

| bakewell 操作 | Sport8 操作 | Meituan 操作 | 说明 |
|--------------|-------------|--------------|------|
| available → locked | 锁定场地 | - | 锁定时段 |
| locked → available | 解锁场地 | - | 释放时段 |
| available → confirmed | 创建预订 | 创建预约 | 新预订 |
| confirmed → cancelled | 取消预订 | 取消预约 | 退款/释放 |

## 实施阶段

### Phase 1: 基础架构 (1-2周)

#### Week 1: StateManager
```
✅ 数据库设计
   - bookings 表
   - operations 表
   - sync_log 表

✅ CRUD 接口
   - get_state()
   - update_state()
   - find_conflicts()
```

#### Week 2: ChangeDetector
```
✅ 轮询检测 bakewell 变更
✅ 变更事件生成
✅ Webhook 接口预留
```

### Phase 2: Sport8 操作器 (2-3周)

#### Week 3: 页面分析
```
✅ 锁定按钮定位
✅ 解锁按钮定位
✅ 确认弹窗处理
✅ 结果验证
```

#### Week 4: 操作实现
```
✅ lock_court() 方法
✅ unlock_court() 方法
✅ 错误处理
✅ 截图记录
```

#### Week 5: 集成测试
```
✅ 端到端测试
✅ 重试机制测试
✅ 并发安全测试
```

### Phase 3: Meituan 操作器 (2周)

#### Week 6-7: 类似实现
```
✅ 页面分析
✅ 操作实现
✅ 集成测试
```

### Phase 4: 冲突解决与优化 (1-2周)

#### Week 8: ConflictResolver
```
✅ 冲突检测算法
✅ 解决策略实现
✅ 手动干预接口
```

#### Week 9: 监控与优化
```
✅ 成功率监控
✅ 延迟优化
✅ 告警配置
```

## 技术选型

| 组件 | 技术 | 理由 |
|------|------|------|
| 数据库 | SQLite | 轻量，无需额外服务 |
| 队列 | Python asyncio Queue | 简单，够用 |
| 页面操作 | Selenium | 成熟，稳定 |
| 调度 | APScheduler | 定时任务 |
| 监控 | Prometheus + Grafana | 行业标准 |

## 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 页面结构变化 | 高 | 定期回归测试，灵活的选择器 |
| 登录失效 | 高 | 自动重登机制，token 刷新 |
| 操作超时 | 中 | 异步处理，超时重试 |
| 数据不一致 | 高 | 定期一致性检查，人工介入 |

## 成功指标

- 同步延迟 < 60秒
- 操作成功率 > 99%
- 数据一致性 100%
- 人工干预 < 1次/天

---

**总结**: 4-6 周完整实施，建议 Phase 1-2 先上线验证稳定性。
